## lambda and functinal interface
- An interface with only one abstract method is called a functional interface.
  but it can have multiple methods as long as only one is abstract.
  `` @FunctionalInterface`` annotation can be used to indicate but it's an optional 

  `` public interface MyFunctionalInterface { ``
  ``        void myMethod();``
  `` }``


- 